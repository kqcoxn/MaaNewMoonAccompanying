# 开发工具

## 调试程序

``` shell
python -m MaaDebugger
```

## 构建程序

``` shell
python ./install.py
```

## 发布

``` shell
git add .
git commit -m "v1.0.1"
git push origin HEAD -u

git tag v1.0.1
git push origin v1.0.1
```
